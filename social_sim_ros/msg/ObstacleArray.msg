# Message defining an array of all obstacles in the environment

Header          header      # Timestamp and frame_id (should be "map")
Obstacle[]      obstacles   # Array containing all tracked obstacles

